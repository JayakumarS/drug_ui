import { CustomerMasterResultBean } from './customer-master-result-bean';

describe('CustomerMasterResultBean', () => {
  it('should create an instance', () => {
    expect(new CustomerMasterResultBean()).toBeTruthy();
  });
});
